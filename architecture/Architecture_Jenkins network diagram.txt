title Jenkins network diagram
Internet or private network {
  API Integration [icon: api]
  End user [icon: firefox]
  Usage statistics [icon: pie-chart]
}

Corporate private network {
  Private Artifact managers SCMs etc [icon: server]
}

Internet {
  Update center [icon: update]
  External Artifact managers SCMs etc [icon: server]
}

SMTP server [icon: server]
SSH Agent [icon: lock]
Inbound Agent [icon: anchor]

Jenkins {
  Jenkins2 [icon: jenkins, label: "Jenkins"]
}

Corporate Proxy (Optional) [icon: http]



// connections
End user > Usage statistics: enabled by default
API Integration <> Jenkins: HTTP (8080) or HTTPS (443)
Internet or private network > Jenkins
Internet or private network --> Jenkins : SSHD disabled by default
Private Artifact managers SCMs etc <> Jenkins : HTTP(S)
Private Artifact managers SCMs etc < Jenkins:SSH
Jenkins > SMTP server:SMTP(25)
Jenkins > SSH Agent:SSH
Jenkins > Inbound Agent:Jenkins Remoting TCP-IP(50000)
Jenkins > Corporate Proxy (Optional):HTTP(S)
Jenkins > Corporate Proxy (Optional):SSH
Corporate Proxy (Optional) > Update center:HTTP(S)
Corporate Proxy (Optional) > External Artifact managers SCMs etc:HTTP(S)
Corporate Proxy (Optional) > External Artifact managers SCMs etc